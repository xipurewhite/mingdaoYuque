# 配置快速参考

本文档提供插件配置的快速参考，方便快速查找配置项说明。

## 📋 配置项清单

> **配置方式说明**：
> - **明道云参数配置界面**：以下所有字段选择器类型的配置项，需要在明道云插件配置界面中通过字段选择器进行配置
> - **环境参数配置**：功能参数部分的两个配置项需要通过环境参数进行配置

### 必填配置（明道云参数配置界面）

| 配置项 | 参数名 | 类型 | 字段类型 | 说明 |
|--------|--------|------|---------|------|
| 文档标题字段 | `title` 或 `titleFieldId` | 字段选择器 | 文本字段 (2) | 用于显示文档标题 |
| 文档内容字段 | `content` 或 `contentFieldId` | 字段选择器 | 富文本字段 (41) | 存储文档内容 |

### 可选配置 - 导航模式（明道云参数配置界面）

#### 传统导航模式（级联选择）

| 配置项 | 参数名 | 类型 | 字段类型 | 说明 |
|--------|--------|------|---------|------|
| 文档分类字段 | `category` 或 `categoryFieldId` | 字段选择器 | 级联选择字段 | 用于构建传统树状导航 |

#### 关联记录导航模式（推荐）

| 配置项 | 参数名 | 类型 | 字段类型 | 说明 |
|--------|--------|------|---------|------|
| 父级关联字段 | `parent_id` 或 `parentFieldId` | 字段选择器 | 关联记录字段 (29/50) | 指向父文档 |
| 子级关联字段 | `children` 或 `childrenFieldId` | 字段选择器 | 关联记录字段 (29/50) | 包含子文档列表 |

### 可选配置 - 功能参数（环境参数配置）

> ⚠️ **注意**：以下两个配置项需要通过环境参数进行配置，而不是在明道云参数配置界面中配置。

| 配置项 | 参数名 | 类型 | 取值 | 说明 |
|--------|--------|------|------|------|
| 访问模式 | `isExternal` | 布尔值 | `true` / `false` | 控制内部/外部访问模式，该参数用于强制性控制，实际插件能自行检查，非必要。 |
| 外部分享URL | `externalShareBaseUrl` | 文本 | URL字符串 | 外部分享基础URL。 |

## 🔧 配置示例

### 环境参数配置示例

以下两个配置项需要通过环境参数进行配置：

```javascript
{
  // 访问模式：控制内部/外部访问模式
  "isExternal": false,  // true: 外部访问模式, false: 内部访问模式
  
  // 外部分享基础URL
  "externalShareBaseUrl": "https://xxxxxx.share.mingdao.net/public/view/2312321312"
}
```

> **说明**：
> - 其他所有配置项（文档标题字段、文档内容字段、导航模式字段等）需要在明道云插件配置界面中通过字段选择器进行配置
> - 环境参数配置的具体方法请参考明道云插件开发文档

## 📝 字段类型对照表

| 类型编号 | 字段类型名称 | 用途 |
|---------|------------|------|
| 2 | 文本字段 | 文档标题 |
| 29 | 关联记录字段 | 父级/子级关联 |
| 41 | 富文本字段 | 文档内容 |
| 50 | 关联记录字段 | 父级/子级关联（新版本） |
| cascade | 级联选择字段 | 文档分类 |

## ⚠️ 注意事项

1. **配置方式区分**：
   - **明道云参数配置界面**：所有字段选择器类型的配置项（文档标题、文档内容、导航模式字段等）需要在明道云插件配置界面中通过字段选择器进行配置
   - **环境参数配置**：功能参数（`isExternal`、`externalShareBaseUrl`）需要通过环境参数进行配置

2. **字段选择器类型配置**：通过明道云参数配置界面配置的参数值以数组格式存储，访问时使用 `env.title?.[0]`

3. **环境参数配置**：
   - 布尔值参数（`isExternal`）：直接以值形式存储，访问时使用 `env.isExternal`
   - 文本参数（`externalShareBaseUrl`）：直接以值形式存储，访问时使用 `env.externalShareBaseUrl`

4. **导航模式选择**：
   - 传统模式：适用于固定分类结构
   - 关联记录模式：适用于动态父子关系（推荐）
   - 两种模式可以同时配置，系统优先使用关联记录模式

## 🔍 配置验证

配置完成后，可以通过以下方式验证：

1. **检查字段类型**：确认字段类型是否正确
2. **测试数据加载**：确认数据能否正常加载
3. **测试导航功能**：确认导航树是否正常显示
4. **检查控制台**：查看是否有错误信息

---

**最后更新**：2025年1月
