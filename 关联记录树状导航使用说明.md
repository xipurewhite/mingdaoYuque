# 关联记录树状导航功能使用说明

## 功能概述

基于关联记录的树状导航功能允许您使用明道云的关联记录字段来构建动态的文档层级结构，替代传统的级联选择字段分类方式。

## 环境变量配置

在明道云插件配置中，需要设置以下环境变量：

```javascript
{
  "titleFieldId": ["66612e51723a2eec263d9e50"],     // 文档标题字段
  "contentFieldId": ["66612f347d1e8e134d1e4604"],   // 文档内容字段
  "categoryFieldId": ["66612f91dc42de6062e40fa0"],  // 层级分类字段（可选，兼容性）
  "parent_id": ["68a689f4b514fbf214695eed"],        // 父级关联记录字段
  "children": ["68a689f4b514fbf214695eee"]          // 子级关联记录字段
}
```

### 字段说明

- **titleFieldId**: 文本字段（类型编号2），用于显示文档标题
- **contentFieldId**: 富文本字段（类型编号41），存储文档内容
- **parent_id**: 关联记录字段（类型编号29或50），指向父文档
- **children**: 关联记录字段（类型编号29或50），包含子文档列表
- **categoryFieldId**: 级联选择字段（可选），用于兼容传统分类方式

## 工作原理

### 1. 数据获取
- 系统首先检查是否配置了关联记录字段（`parent_id` 和 `children`）
- 如果配置了关联记录字段，优先使用关联记录树状结构
- 如果没有配置，则回退到传统的级联选择分类方式

### 2. 树状结构构建
- 解析每个文档的 `parent_id` 字段，建立父子关系
- 解析每个文档的 `children` 字段，确定子节点信息
- 自动检测循环引用和孤儿节点
- 将孤儿节点归入"未分类文档"分组

### 3. 懒加载机制
- 大数据量时，子节点按需加载
- 使用缓存机制减少重复请求
- 支持展开/折叠操作

## 性能优化

### 1. 缓存系统
- **树状结构缓存**: 缓存整个树状结构，避免重复构建
- **子节点缓存**: 缓存已加载的子节点数据
- **展开状态缓存**: 记住用户的展开状态
- **自动过期**: 基于数据更新时间自动失效缓存

### 2. 懒加载策略
- **小数据量（≤2000条）**: 一次性构建完整树状结构
- **大数据量（>2000条）**: 根节点立即显示，子节点按需加载
- **预加载**: 展开节点时预加载下一层子节点

### 3. 错误处理
- **参数验证**: 验证所有输入参数的有效性
- **数据验证**: 检查文档数据的完整性
- **异常捕获**: 捕获并处理各种异常情况
- **错误边界**: 使用React错误边界防止组件崩溃

## 使用场景

### 1. 知识库管理
- 建立层级化的知识结构
- 支持动态调整文档关系
- 便于知识分类和检索

### 2. 文档集管理
- 组织相关文档
- 建立文档间的引用关系
- 支持文档版本管理

### 3. 项目管理
- 建立项目文档结构
- 支持子项目和任务文档
- 便于项目进度跟踪

## 配置示例

### 明道云工作表结构
```
工作表: 文档库
├── 标题 (文本字段)
├── 内容 (富文本字段)
├── 父级文档 (关联记录字段) -> 指向同一工作表的其他记录
└── 子级文档 (关联记录字段) -> 指向同一工作表的其他记录
```

### 数据关系示例
```
根文档A
├── 子文档A1
│   ├── 子文档A1-1
│   └── 子文档A1-2
└── 子文档A2

根文档B
└── 子文档B1
```

## 兼容性说明

### 1. 向后兼容
- 保留对传统级联选择字段的支持
- 可以同时使用两种分类方式
- 平滑迁移，无需修改现有数据

### 2. 混合模式
- 支持关联记录和级联选择混合使用
- 优先使用关联记录，回退到级联选择
- 灵活配置，适应不同需求

## 故障排除

### 1. 常见问题

**Q: 树状结构没有显示**
A: 检查是否正确配置了 `parent_id` 和 `children` 字段，确保字段类型为关联记录字段（29或50）

**Q: 子节点加载失败**
A: 检查网络连接和API权限，查看控制台错误信息

**Q: 循环引用问题**
A: 系统会自动检测并处理循环引用，将异常节点归入"未分类文档"

**Q: 缓存问题**
A: 缓存会自动过期，也可以手动清除浏览器缓存

### 2. 调试信息
- 打开浏览器开发者工具查看控制台日志
- 检查网络请求是否成功
- 验证字段配置是否正确

### 3. 性能问题
- 大数据量时启用懒加载
- 检查缓存是否正常工作
- 考虑分批加载数据

## 最佳实践

### 1. 数据设计
- 合理设计文档层级结构
- 避免过深的嵌套层级
- 定期清理无效的关联关系

### 2. 性能优化
- 控制单次加载的数据量
- 合理使用缓存机制
- 定期清理过期缓存

### 3. 用户体验
- 提供清晰的加载状态指示
- 支持键盘导航
- 保持展开状态的记忆

## 技术架构

### 1. 核心组件
- `RelationTreeNavigation`: 关联记录树状导航主组件
- `RelationTreeNode`: 树节点组件
- `TreeErrorBoundary`: 错误边界组件

### 2. 工具函数
- `buildRelationTree`: 构建树状结构
- `parseRelationData`: 解析关联记录数据
- `loadChildNodes`: 懒加载子节点
- `detectCircularReferences`: 检测循环引用

### 3. 缓存管理
- `TreeCacheManager`: 缓存管理器
- 支持多种缓存类型
- 自动过期和清理机制

## 更新日志

### V1.0.0 (2024-12)
- 初始版本发布
- 支持基于关联记录的树状导航
- 实现懒加载和缓存机制
- 添加错误处理和健壮性保障
- 支持性能优化和虚拟化渲染

---

如有问题或建议，请联系开发团队。
